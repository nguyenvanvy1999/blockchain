export enum ECollectionName {
  USER = 'users',
  SETTING = 'settings',
  LOGGER = 'loggers',
  MESSAGE = 'messages',
  CONVERSATION = 'conversations',
  POST = 'posts',
  SOCKET = 'sockets',
  VOTE = 'votes',
}
